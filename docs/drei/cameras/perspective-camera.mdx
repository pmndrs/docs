---
title: PerspectiveCamera
---

A responsive `THREE.PerspectiveCamera` that can set itself as the default.

```jsx
<PerspectiveCamera
  makeDefault // Registers it as the default camera system-wide (default=false)
  {...props} // All THREE.PerspectiveCamera props are valid
>
  <mesh />
</PerspectiveCamera>
```

<StoryBookLink link="camera-perspectivecamera--perspective-camera-scene-st" />

<StoryBookEmbed id="camera-perspectivecamera--perspective-camera-scene-st" />
