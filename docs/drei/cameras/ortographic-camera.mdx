---
title: OrthographicCamera
---

A responsive `THREE.OrthographicCamera` that can set itself as the default.

```jsx
<OrthographicCamera
  makeDefault // Registers it as the default camera system-wide (default=false)
  {...props} // All THREE.OrthographicCamera props are valid
>
  <mesh />
</OrthographicCamera>
```

<StoryBookLink link="camera-orthographiccamera--orthographic-camera-scene-st" />

<StoryBookEmbed id="camera-orthographiccamera--orthographic-camera-scene-st" />
