---
title: useTexture
nav: 48
---

A convenience hook that uses `useLoader` and `TextureLoader`

```jsx
const texture = useTexture(url)
const [texture1, texture2] = useTexture([texture1, texture2])
```

<StoryBook lib="drei" id="loaders-texture" />
