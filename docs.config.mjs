/**
 * Documentation configuration
 * This file documents available configuration options for the documentation generator
 */

/**
 * External MDX components
 * 
 * These are optional components that can be enabled/disabled via environment variables.
 * When disabled, their dependencies won't be required.
 * 
 * Available external components:
 * - sandpack: Interactive code sandboxes (requires @codesandbox/sandpack-react)
 * 
 * Configuration via environment variables:
 * - EXTERNAL_SANDPACK: Set to 'false' to disable Sandpack component
 * 
 * Example:
 * ```sh
 * export EXTERNAL_SANDPACK=false  # Disable Sandpack
 * npm run build
 * ```
 * 
 * By default, all external components are enabled for backward compatibility.
 */

// This file is for documentation purposes only.
// Configuration is done via environment variables as documented above.
