/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Tailwind CSS v4 has its own built-in CSS processor
    // and doesn't need to be configured as a PostCSS plugin
  },
}

export default config
